# 🚀 Как запустить Lofi Radio

## Быстрый способ:

### 1. Запусти локальный сервер:

```powershell
.\start-server.ps1
```

Или вручную:
```powershell
cd public
python -m http.server 8000
```

### 2. Открой в браузере:

```
http://localhost:8000
```

---

## Почему нужен сервер?

Браузер блокирует загрузку JSON файлов через `file://` протокол из-за политики безопасности CORS. Локальный HTTP сервер решает эту проблему.

---

## Альтернатива (без сервера):

Если нет Python, можно использовать:
- **Node.js**: `npx http-server public -p 8000`
- **PHP**: `php -S localhost:8000` (в папке public)
- **VS Code**: расширение "Live Server"

---

**Готово!** После запуска сервера открой `http://localhost:8000` и наслаждайся музыкой! 🎵

